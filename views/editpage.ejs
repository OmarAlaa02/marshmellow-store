<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <link rel="stylesheet" href="/edit-product.css">
    <meta name="csrf-token" content="<%= csrfToken %>">
</head>
<body>
    
    <div class="edit-product-container">
        <h2>Edit Product</h2>

        <form id="product-form" enctype="multipart/form-data">
           
            <input type="hidden" id="product-id" name="productId" value="<%= product._id %>"> 
           

            <div class="form-group">
                <label for="product-name">Product Name</label>
                <input type="text" id="product-name" name="name" required value="<%= product.name %>">
            </div>

            <div class="form-group">
                <label for="product-price">Price</label>
                <input type="number" id="product-price" name="price" required value="<%= product.price %>">
            </div>

            <div class="form-group">
                <label for="product-image">Product Image</label>
                <input type="file" id="product-image" name="imgURL" >
            </div>
            <input type="hidden" value="<%=product.imgUrl%>" name="imgUrl">
            <button type="submit">Edit Product</button>
        </form>
    </div>

    <script src="/scripts/editproduct.js">
        
    </script>
</body>
</html>
